# Auto-generated at build time by build.zig.
# Set ANDROID_NDK_HOME and ANDROID_API_LEVEL env vars, then run:
#   zig build shared-lib -Dtarget=aarch64-linux-android
include_dir=
sys_include_dir=
crt_dir=
msvc_lib_dir=
kernel32_lib_dir=
gcc_dir=
